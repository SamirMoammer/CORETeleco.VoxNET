@model List<CORETeleco.Models.ComprobanteModel>
@{
    ViewData["Title"] = "Listar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card">
    <div class="card-header">
        Lista de Comprobantes
    </div>
    <div class="card-body">
        <a asp-action="Guardar" asp-controller="Comprobante" class="btn btn-success"><i class="bi bi-plus-circle"></i> Crear Nuevo</a>
        <hr />
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Tipo de Comprobante</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.tipoComprobante</td>
                        <td>
                            <a asp-action="Editar" asp-controller="Comprobante" class="btn btn-primary btn-sm" asp-route-idTipoComprobante="@item.idTipoComprobante">Editar</a>
                            <a asp-action="Eliminar" asp-controller="Comprobante" class="btn btn-danger btn-sm" asp-route-idTipoComprobante="@item.idTipoComprobante">Eliminar</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
