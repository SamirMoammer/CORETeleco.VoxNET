@model ComprobanteModel
@{
    ViewData["Title"] = "Guardar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card">
    <div class="card-header">
        Crear Comprobante
    </div>
    <div class="card-body">
        <form asp-action="Guardar" asp-controller="Comprobante" method="post">
            <div class="mb-3">
                <label class="form-label">Tipo de Comprobante</label>
                <input asp-for="tipoComprobante" type="text" class="form-control">
                <span asp-validation-for="tipoComprobante" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-primary">Guardar</button>
            <a asp-action="Listar" asp-controller="Comprobante" class="btn btn-warning">Volver a la lista</a>
        </form>
    </div>
</div>
