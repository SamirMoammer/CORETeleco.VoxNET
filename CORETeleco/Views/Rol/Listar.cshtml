@model List<CORETeleco.Models.RolModel>
@{
    ViewData["Title"] = "Listar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card">
    <div class="card-header">
        Lista de Roles
    </div>
    <div class="card-body">
        <a asp-action="Guardar" asp-controller="Rol" class="btn btn-success"><i class="bi bi-plus-circle"></i> Crear Nuevo</a>
        <hr />
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.nombreRol</td>
                        <td>
                            <a asp-action="Editar" asp-controller="Rol" class="btn btn-primary btn-sm" asp-route-idRol="@item.idRol">Editar</a>
                            <a asp-action="Eliminar" asp-controller="Rol" class="btn btn-danger btn-sm" asp-route-idRol="@item.idRol">Eliminar</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
