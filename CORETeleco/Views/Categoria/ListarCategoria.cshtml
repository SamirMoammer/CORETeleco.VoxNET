@model List<CORETeleco.Models.CategoriaModel>
@{
    ViewData["Title"] = "Listar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card">
    <div class="card-header">
        Lista de Categorías
    </div>
    <div class="card-body">
        <a asp-action="Guardar" asp-controller="Categoria" class="btn btn-success"><i class="bi bi-plus-circle"></i> Crear Nuevo</a>
        <hr />
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Categoría</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.categoriaProducto</td>
                        <td>
                            <a asp-action="Editar" asp-controller="Categoria" class="btn btn-primary btn-sm" asp-route-idCategoriaProducto="@item.idCategoriaProducto">Editar</a>
                            <a asp-action="Eliminar" asp-controller="Categoria" class="btn btn-danger btn-sm" asp-route-idCategoriaProducto="@item.idCategoriaProducto">Eliminar</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

