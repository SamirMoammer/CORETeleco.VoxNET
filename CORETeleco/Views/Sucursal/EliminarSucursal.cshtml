@model SucursalModel
@{
    ViewData["Title"] = "Eliminar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card">
    <div class="card-header">
        Eliminar Sucursal
    </div>
    <div class="card-body">
        <form asp-action="Eliminar" asp-controller="Sucursal" method="post">
            <input asp-for="idSucursal" type="hidden" class="form-control">
            <div class="alert alert-danger" role="alert">
                ¿Desea eliminar la sucursal: @Html.DisplayTextFor(m => m.nombreSucursal)?
            </div>
            <button type="submit" class="btn btn-danger">Eliminar</button>
            <a asp-action="Listar" asp-controller="Sucursal" class="btn btn-warning">Volver a la lista</a>
        </form>
    </div>
</div>
